# TP01 Canonical GitHub Workflow

version: 2.2.0
status: active
canonical_source: GitHub
repository: hoiyu915-droid/Tp01
branch: main
source_lock: TP01_SOURCE_LOCK.md
updated_at: 2026-06-30

---

## 0. Purpose

TP01 is the daily-use anchor extraction workflow for turning one or more reference images into a reusable Anchor Set.

This file is the executable procedure. It must be read from GitHub when the user invokes `TP01` / `tp01` without attaching a local TP01 file.

TP01 no longer uses Google Drive as the default source for the procedure file or seed anchors.

Version 2.2.0 adds a mandatory runtime gate after Stop Point 1. After source reading passes, TP01 must generate `AnchorA.runtime.md`, `AnchorB.runtime.md`, and `AnchorC.runtime.md` from the current user source inputs before any anchor image generation. This prevents canonical baseline anchors from being mistaken for the current-run extraction target.

---

## 1. Source Authority

Canonical source order:

```text
1. GitHub repo metadata
2. TP01_SOURCE_LOCK.md
3. Tp01.md
4. raw AnchorA.PNG / AnchorB.PNG / AnchorC.PNG
```

Required canonical raw links:

```text
Tp01.md
https://raw.githubusercontent.com/hoiyu915-droid/Tp01/main/Tp01.md

AnchorA.PNG
https://raw.githubusercontent.com/hoiyu915-droid/Tp01/main/AnchorA.PNG

AnchorB.PNG
https://raw.githubusercontent.com/hoiyu915-droid/Tp01/main/AnchorB.PNG

AnchorC.PNG
https://raw.githubusercontent.com/hoiyu915-droid/Tp01/main/AnchorC.PNG
```

The GitHub connector is used for:

```text
- repository metadata
- file SHA
- Tp01.md text
- TP01_SOURCE_LOCK.md text
- text/procedure audit
```

Raw GitHub links are used for:

```text
- AnchorA.PNG visual content
- AnchorB.PNG visual content
- AnchorC.PNG visual content
```

Important distinction:

```text
File existence / SHA / metadata is not visual read.
An anchor may be called "read" only after the actual image content has been opened or visually inspected.
```

Canonical anchor files define role grammar only. They are not current-run content.

```text
AnchorA.PNG baseline = what a layout grammar anchor is
AnchorB.PNG baseline = what a style DNA anchor is
AnchorC.PNG baseline = what an asset / object vocabulary anchor is
```

The current run must be derived from the current user source inputs through runtime files before image generation.

---

## 2. No-GDrive Default Rule

When the user invokes `TP01` / `tp01` without attachments:

```text
Do not use GDrive as the default TP01 template source.
Do not use GDrive as the default seed anchor source.
Do not search Drive for the TP01 procedure.
Do not search Drive for AnchorA/B/C.
Do not silently fall back to Drive if GitHub raw images fail.
```

GDrive may only be used when the user explicitly says one of the following:

```text
upload to Drive
Drive audit
Drive verification
use GDrive TP01
use Drive anchors
check the Drive folder
```

If the user explicitly requests Drive, that instruction overrides this default.

---

## 3. Required Files In This Repo

A valid TP01 repo must contain:

```text
TP01_SOURCE_LOCK.md
Tp01.md
AnchorA.PNG
AnchorB.PNG
AnchorC.PNG
```

Recommended documentation files:

```text
README.md
TP01_RUNTIME_MANIFEST.md
```

---

## 4. Invocation Contract

When the user says `tp01` and does not attach a TP01 file:

1. Fetch repository metadata from `hoiyu915-droid/Tp01`.
2. Read `TP01_SOURCE_LOCK.md`.
3. Read `Tp01.md`, including SHA / metadata.
4. Read AnchorA / AnchorB / AnchorC from the canonical raw links.
5. Verify all four source components are available:
   - `Tp01.md`
   - `AnchorA.PNG`
   - `AnchorB.PNG`
   - `AnchorC.PNG`
6. **STOP POINT 1 — Anchor Read Report**
   - Before any runtime or generation, output a report confirming whether AnchorA / B / C were actually read as visual content.
   - This report must distinguish:
     - file existence / metadata
     - SHA availability
     - actual visual-read success
     - unresolved failures
   - If any anchor image was not actually read visually, stop the TP01 run here.
7. Inspect the current user reference / task only after Stop Point 1 passes.
8. Generate current-run runtime files from the current source inputs:
   - `AnchorA.runtime.md`
   - `AnchorB.runtime.md`
   - `AnchorC.runtime.md`
9. **STOP POINT 1R — Runtime Report**
   - Evaluate whether each runtime was derived from the current source input.
   - Confirm whether:
     - AnchorA runtime is layout grammar only
     - AnchorB runtime is style DNA only
     - AnchorC runtime is asset / object vocabulary only
     - the three runtimes are sufficiently separated
     - no runtime silently reverted to canonical baseline content
   - Do not generate anchor images before this report passes.
10. Create `SOURCE_DIR / SOURCE_INDEX.md` draft if creating a package.
11. Generate `AnchorA.PNG` sequentially from `AnchorA.runtime.md`.
12. Validate `AnchorA.PNG`.
13. Generate `AnchorB.PNG` sequentially from `AnchorB.runtime.md`.
14. Validate `AnchorB.PNG`.
15. Generate `AnchorC.PNG` sequentially from `AnchorC.runtime.md`.
16. Validate `AnchorC.PNG`.
17. **STOP POINT 2 — Generated Anchor Report**
   - After generating A / B / C, output a report evaluating:
     - whether AnchorA is layout grammar only
     - whether AnchorB is style DNA only
     - whether AnchorC is asset / object vocabulary only
     - whether the three anchors are sufficiently separated
     - whether any anchor drifted into a finished card or full story
     - whether each anchor follows its runtime
   - Do not write `manifest.md`, finalize `SOURCE_INDEX.md`, create a zip, or upload before this report passes.
18. Finalize `SOURCE_DIR/SOURCE_INDEX.md` only after Stop Point 2 passes.
19. Write `manifest.md` only after Stop Point 2 passes.
20. **STOP POINT 3 — Package Preflight Report**
   - Before building the final seed package / zip, output a report confirming:
     - all required files exist
     - source references are recorded
     - runtime files are recorded
     - manifest is aligned with A / B / C and runtime
     - no overview image replaced A / B / C
     - package is complete and ready
21. Generate zip / upload only after Stop Point 3 passes and only if the user requested a package / zip / upload.
22. Report any failed source explicitly.
23. Do not substitute GDrive unless the user explicitly asks for it.

Hard rule:

```text
Do not collapse TP01 into a single uninterrupted generate-and-package run.
Do not generate anchors before AnchorA/B/C runtime exists.
The stop points and runtime gate are mandatory execution gates.
```

---

## 5. Output Contract

Each TP01 extraction run produces a complete Anchor Package.

Required outputs:

```text
AnchorA.runtime.md
AnchorB.runtime.md
AnchorC.runtime.md
AnchorA.PNG
AnchorB.PNG
AnchorC.PNG
manifest.md
```

Strict rules:

```text
- AnchorA.runtime.md, AnchorB.runtime.md, and AnchorC.runtime.md must exist before anchor image generation.
- AnchorA.PNG, AnchorB.PNG, and AnchorC.PNG must be three separate image files.
- Do not combine A/B/C into one overview image.
- Do not replace the three images with manifest.md.
- Do not crop the original reference directly and call it an anchor.
- Do not use canonical baseline anchor content as current-run extraction content.
- Each anchor has one job only.
- Generate complete files directly; do not output patch-only files.
```

Optional local package structure:

```text
TP01_OUTPUT/
  SOURCE_DIR/
    SOURCE_INDEX.md
    REF_01.<ext>
    REF_02.<ext>
  AnchorA.runtime.md
  AnchorB.runtime.md
  AnchorC.runtime.md
  AnchorA.PNG
  AnchorB.PNG
  AnchorC.PNG
  manifest.md
```

If the user asks for a zipped package, produce a complete zip containing the full folder, not partial patches.

---

## 6. Anchor Separation Model

### AnchorA.PNG — layout grammar only

Purpose:

```text
Extract page skeleton, reading order, spacing, panels, card hierarchy, and content zones.
For non-card references, extract the source image's spatial grammar: frame ratio, viewpoint, axes, depth layers, node positions, negative space, flow, repetition, and structural rhythm.
```

Must include:

```text
- page ratio or frame ratio
- viewpoint
- primary structure
- depth layers
- dominant axes
- major nodes
- negative space map
- reading / viewing flow
- repetition or rhythm
- structural relationships
```

May include when the output target is an infographic card:

```text
- title zone
- main content zone
- visual / character placeholder zone
- supporting note zone
- conclusion / takeaway zone
- arrows, panels, grid, card relationship
- whitespace ratio
- reading order
```

Must not include:

```text
- complete character
- complete object
- readable final text
- original topic content
- finished illustration
- brand / identity carryover
- canonical baseline layout copied as the current-run content
```

Failure condition:

```text
If AnchorA looks like a finished infographic rather than a reusable skeleton, it fails.
If AnchorA reflects the canonical baseline instead of the current source input, it fails.
```

---

### AnchorB.PNG — style DNA only

Purpose:

```text
Extract visual language without fixing the final layout.
```

Must include:

```text
- line quality
- palette
- typography feel if present
- icon language if present
- character proportion if present
- expression grammar if present
- border style if present
- decoration style if present
- material impression
- texture language
- surface character
- contrast profile
- overall visual tone
```

Must not include:

```text
- fixed full layout
- complete infographic content
- original story
- object inventory
- workflow logic
- topic conclusion
- canonical baseline style copied as the current-run content
```

Failure condition:

```text
If AnchorB becomes a full information card or simply a recolored copy of the reference, it fails.
If AnchorB reflects the canonical baseline instead of the current source input, it fails.
```

---

### AnchorC.PNG — asset / object vocabulary only

Purpose:

```text
Extract reusable visual components and object vocabulary.
```

Must include when present in the source:

```text
- poses
- expressions
- gestures
- props
- scene elements
- icons
- arrows
- labels
- cards
- reusable UI / infographic components
- primary objects
- secondary objects
- connectors or relationships
- micro details
```

Must not include:

```text
- complete page composition
- complete story
- full hierarchy
- single fixed conclusion
- brand-specific or identity-specific objects copied from the source
- canonical baseline assets copied as the current-run content
```

Failure condition:

```text
If AnchorC reads like a finished story scene instead of a reusable asset sheet, it fails.
If AnchorC reflects the canonical baseline instead of the current source input, it fails.
```

---

## 7. Runtime Contract

Before generating `AnchorA.PNG`, `AnchorB.PNG`, or `AnchorC.PNG`, TP01 must generate three runtime files derived from the current source inputs.

Required runtime outputs:

```text
AnchorA.runtime.md
AnchorB.runtime.md
AnchorC.runtime.md
```

These runtime files are mandatory intermediate artifacts.

Purpose:

```text
- convert current reference inputs into explicit extraction rules
- prevent silent fallback to canonical baseline content
- separate source interpretation from image generation
- make A / B / C generation auditable
```

Hard rule:

```text
Do not generate AnchorA.PNG before AnchorA.runtime.md exists.
Do not generate AnchorB.PNG before AnchorB.runtime.md exists.
Do not generate AnchorC.PNG before AnchorC.runtime.md exists.
Canonical anchor files define role grammar only; they do not substitute for current-run runtime.
```

### AnchorA.runtime.md — layout extraction runtime

Required fields:

```md
# AnchorA Runtime

anchor_role: layout grammar only
source_refs:
source_summary:
viewpoint:
frame_ratio:
primary_structure:
depth_layers:
dominant_axes:
major_nodes:
negative_space_map:
reading_flow:
repetition_or_rhythm:
must_capture:
must_avoid:
abstraction_rule:
generation_rule:
validation_checks:
status:
```

### AnchorB.runtime.md — style DNA runtime

Required fields:

```md
# AnchorB Runtime

anchor_role: style DNA only
source_refs:
source_summary:
material_impression:
texture_language:
line_or_edge_quality:
shape_language:
tone_or_mood:
palette_family:
contrast_profile:
surface_character:
must_capture:
must_avoid:
abstraction_rule:
generation_rule:
validation_checks:
status:
```

### AnchorC.runtime.md — asset / object vocabulary runtime

Required fields:

```md
# AnchorC Runtime

anchor_role: asset / object vocabulary only
source_refs:
source_summary:
primary_objects:
secondary_objects:
connectors_or_relationships:
reusable_components:
gesture_or_pose_vocabulary:
scene_elements:
micro_details:
must_capture:
must_avoid:
abstraction_rule:
generation_rule:
validation_checks:
status:
```

Runtime generation rule:

```text
Runtime must be derived from current source input(s), not from memory, not from old packages, and not from canonical anchor role descriptions alone.
```

---

## 8. Manifest Contract

Every TP01 run must produce one `manifest.md`.

Required fields:

```text
# TP01 Anchor Package Manifest

seed_name:
role:
canonical_status:
default_ratio:
source_policy:
created_at:

## Source Inputs

## Runtime Outputs
- AnchorA.runtime.md:
- AnchorB.runtime.md:
- AnchorC.runtime.md:

## Suitable For

## Not Suitable For

## Visual Parameters
- layout_density:
- character_load:
- mechanism_capacity:
- evidence_capacity:
- object_capacity:
- text_capacity:
- workflow_capacity:
- tone:

## Anchor Outputs
- AnchorA.PNG: layout grammar only
- AnchorB.PNG: style DNA only
- AnchorC.PNG: object / scene vocabulary only

## Anchor Use
- AnchorA:
- AnchorB:
- AnchorC:

## Runtime-to-Anchor Mapping

## Routing Rule

## Governance Rules

## Validation Status
```

Manifest must not replace the three anchor images or three runtime files.

---

## 9. Source Registry Contract

For each new or revised anchor package, create or update:

```text
SOURCE_DIR/SOURCE_INDEX.md
```

Schema:

```md
# SOURCE_INDEX

source_policy: GitHub canonical TP01 unless user explicitly overrides
last_updated: <YYYY-MM-DD>

## Reference Registry

| ref_id | file_or_url | source_type | used_for | extraction_note | status |
|---|---|---|---|---|---|
| REF_01 | <file or url> | image / screenshot / url / note / prompt | A+B+C+runtime+manifest | <what was extracted> | active |

## Extraction Mapping

### AnchorA Layout References
- REF_01: <layout grammar extracted>

### AnchorB Style References
- REF_01: <style DNA extracted>

### AnchorC Asset References
- REF_01: <asset vocabulary extracted>

### Runtime References
- AnchorA.runtime.md: <current-run layout runtime derived from REF set>
- AnchorB.runtime.md: <current-run style runtime derived from REF set>
- AnchorC.runtime.md: <current-run asset runtime derived from REF set>

### Manifest / Routing References
- REF_01: <routing and use-case extracted>

## Update Log
- <YYYY-MM-DD>: created or updated anchor package.
```

Allowed `used_for` values:

```text
A
B
C
runtime
manifest
A+B
A+C
B+C
A+B+C
A+B+C+runtime
A+B+C+runtime+manifest
```

Allowed source types:

```text
image
screenshot
url
chatgpt_output
prompt
note
```

`drive_file` is not a default source type. Use it only when the user explicitly provides or requests Drive material.

---

## 10. Default Style Policy

Unless the user explicitly gives another style, new TP01 anchor packages should use:

```text
- portrait / vertical layout by default
- soft Japanese hand-drawn card style
- clean readable structure
- low-saturation palette
- fine linework
- white or pale paper background
- rounded modules
- social-card readability
```

Default transformation rule:

```text
copy structure, not identity
copy workflow logic, not brand
copy visual function, not exact artwork
copy mood category, not artist-specific style
copy current source extraction through runtime, not canonical baseline content
```

---

## 11. Execution Order

TP01 must be executed sequentially:

```text
STEP 0  -> read TP01_SOURCE_LOCK.md
STEP 1  -> read Tp01.md / SHA / metadata
STEP 2  -> read raw AnchorA.PNG
STEP 3  -> read raw AnchorB.PNG
STEP 4  -> read raw AnchorC.PNG
STEP 5  -> STOP POINT 1: Anchor Read Report
STEP 6  -> inspect current user reference / task
STEP 7  -> generate AnchorA.runtime.md
STEP 8  -> validate AnchorA.runtime.md
STEP 9  -> generate AnchorB.runtime.md
STEP 10 -> validate AnchorB.runtime.md
STEP 11 -> generate AnchorC.runtime.md
STEP 12 -> validate AnchorC.runtime.md
STEP 13 -> STOP POINT 1R: Runtime Report
STEP 14 -> create SOURCE_DIR / SOURCE_INDEX.md draft if creating a package
STEP 15 -> generate AnchorA.PNG from AnchorA.runtime.md
STEP 16 -> validate AnchorA.PNG
STEP 17 -> generate AnchorB.PNG from AnchorB.runtime.md
STEP 18 -> validate AnchorB.PNG
STEP 19 -> generate AnchorC.PNG from AnchorC.runtime.md
STEP 20 -> validate AnchorC.PNG
STEP 21 -> STOP POINT 2: Generated Anchor Report
STEP 22 -> finalize SOURCE_INDEX.md
STEP 23 -> write manifest.md
STEP 24 -> STOP POINT 3: Package Preflight Report
STEP 25 -> final package validation
STEP 26 -> zip or upload only if explicitly requested
```

Do not skip from reference image directly to final card.

Do not make AnchorB before AnchorA.

Do not make AnchorC before AnchorB.

Do not write manifest before A/B/C and A/B/C runtime exist, unless the user explicitly asks for a planning-only manifest.

Do not zip or upload before Stop Point 3 passes.

Strict gate rule:

```text
Do not skip Stop Point 1.
Do not generate runtime before the Anchor Read Report.
Do not generate anchors before the Runtime Report.
Do not generate the seed package before the Generated Anchor Report.
Do not zip or upload before the Package Preflight Report.
```

---

## 12. Validation Checklist

Before finalizing a TP01 package, confirm:

```text
[ ] TP01_SOURCE_LOCK.md was read from GitHub.
[ ] Tp01.md was read from GitHub.
[ ] Tp01.md SHA / metadata were checked.
[ ] AnchorA.PNG was visually read through raw GitHub link.
[ ] AnchorB.PNG was visually read through raw GitHub link.
[ ] AnchorC.PNG was visually read through raw GitHub link.
[ ] Stop Point 1 report was issued.
[ ] No GDrive source was used unless explicitly requested.
[ ] AnchorA.runtime.md exists.
[ ] AnchorB.runtime.md exists.
[ ] AnchorC.runtime.md exists.
[ ] Runtime for A/B/C was derived from current source inputs.
[ ] Runtime separation integrity passed.
[ ] Stop Point 1R report was issued.
[ ] SOURCE_INDEX.md exists if creating a package.
[ ] AnchorA.PNG is layout only.
[ ] AnchorB.PNG is style DNA only.
[ ] AnchorC.PNG is asset vocabulary only.
[ ] A/B/C are separate files.
[ ] Runtime-to-anchor fidelity passed.
[ ] Stop Point 2 report was issued.
[ ] manifest.md exists.
[ ] Stop Point 3 report was issued.
[ ] No combined overview image replaces A/B/C.
[ ] No original text, brand, or identity is copied by default.
[ ] Output is complete, not patch-only.
```

---

## 13. Failure Handling

If any required GitHub source fails:

```text
- Say exactly which file failed.
- Distinguish metadata success from visual-read success.
- Do not claim an anchor was "read" if only SHA / existence was confirmed.
- Do not infer anchor visual content from Tp01.md text alone.
- Do not infer anchor visual content from memory, prior conversation, or previous seed packages.
- Do not silently search GDrive.
- Ask for explicit override only if the user wants to continue with another source.
- Stop before runtime generation if Stop Point 1 cannot pass.
```

If runtime generation fails:

```text
- Say exactly which runtime failed.
- Distinguish source-read success from runtime-derivation success.
- Do not generate the corresponding anchor image.
- Do not infer runtime from canonical anchor role definitions alone.
- Do not silently reuse old runtime, old package, or old output.
- Stop before AnchorA / B / C image generation if Stop Point 1R cannot pass.
```

If generated anchors fail separation rules:

```text
- Stop after Stop Point 2.
- Report which anchor failed and why.
- Regenerate the failed anchor only, using the corresponding runtime.
- Do not write manifest or build package until AnchorA / B / C all pass.
```

If package assembly fails:

```text
- Stop at Stop Point 3.
- Report missing or inconsistent files.
- Do not zip incomplete outputs.
```

Common failures and corrections:

```text
Tp01.md still mentions Drive as default -> rewrite procedure to GitHub canonical source.
runtime copied canonical baseline instead of current source -> regenerate runtime from current REF set.
AnchorA.runtime contains style or object inventory -> reduce to layout grammar only.
AnchorB.runtime contains fixed page skeleton -> remove composition lock.
AnchorC.runtime contains full story -> split into reusable object vocabulary.
runtime missing must_capture / must_avoid / generation_rule -> complete the runtime schema.
runtime not created but anchor generated -> invalid run; discard anchor and regenerate from runtime stage.
AnchorA too finished -> redo as layout skeleton.
AnchorB includes full layout -> remove fixed composition.
AnchorC forms a complete story -> split into reusable asset vocabulary.
manifest too vague -> add routing, governance, A/B/C separation, runtime mapping, validation status.
only one image generated -> split into AnchorA/B/C.
only SHA was read -> report metadata-only state and stop before runtime generation.
overview image generated -> reject and regenerate A/B/C separately.
```

---

## 14. Stop Point Report Contract

### STOP POINT 1 — Anchor Read Report

Must report:

```text
- repository:
- Tp01.md sha:
- AnchorA sha:
- AnchorB sha:
- AnchorC sha:
- AnchorA metadata/existence: success / failed
- AnchorB metadata/existence: success / failed
- AnchorC metadata/existence: success / failed
- AnchorA visual read: success / failed
- AnchorB visual read: success / failed
- AnchorC visual read: success / failed
- unresolved gaps:
- permission to continue to runtime: yes / no
```

Rules:

```text
- If visual read is failed for any anchor, permission to continue must be no.
- Do not generate runtime from memory, old seed, or role definitions.
- Do not treat Tp01.md role descriptions as proof of current-run source interpretation.
```

### STOP POINT 1R — Runtime Report

Must report:

```text
- AnchorA.runtime.md status: pass / fail
- AnchorB.runtime.md status: pass / fail
- AnchorC.runtime.md status: pass / fail
- current-source derivation integrity: pass / fail
- runtime separation integrity: pass / fail
- baseline fallback detected: yes / no
- notable drift:
- fixes required:
- permission to continue to anchor generation: yes / no
```

Rules:

```text
- If any runtime fails, permission to continue must be no.
- Do not generate AnchorA.PNG / AnchorB.PNG / AnchorC.PNG before this report passes.
- Runtime must be auditable against current source references.
```

### STOP POINT 2 — Generated Anchor Report

Must report:

```text
- AnchorA status: pass / fail
- AnchorB status: pass / fail
- AnchorC status: pass / fail
- separation integrity: pass / fail
- runtime-to-anchor fidelity: pass / fail
- notable drift:
- fixes required:
- permission to continue to manifest/package: yes / no
```

Rules:

```text
- If any anchor fails, permission to continue must be no.
- Do not generate manifest or package before this report passes.
```

### STOP POINT 3 — Package Preflight Report

Must report:

```text
- files present:
- SOURCE_INDEX.md status:
- runtime files status:
- manifest.md status:
- runtime-anchor-manifest alignment:
- packaging completeness:
- zip readiness: yes / no
```

Rules:

```text
- If any required file is missing or inconsistent, zip readiness must be no.
- Do not create a zip or upload incomplete outputs.
```

---

## 15. One-Line Invocation

```text
依照 GitHub canonical TP01：先讀 TP01_SOURCE_LOCK.md，再讀 Tp01.md / SHA / metadata，然後用 raw AnchorA/B/C 作為 anchor role baseline；不使用 GDrive，除非我明講 upload、audit 或 Drive verification。第一個停頓點先輸出 AnchorA/B/C 讀入報告；通過後，根據本次 source inputs 生成 AnchorA.runtime.md、AnchorB.runtime.md、AnchorC.runtime.md，並輸出 runtime report。只有 runtime report 通過後，才依序生成 AnchorA、AnchorB、AnchorC。第二個停頓點先輸出 generated anchor report；第三個停頓點在生成整個 seed package / zip 前輸出 package preflight report。全部通過後，才輸出完整檔案，不做 patch。
```

---

## 16. Current Canonical Anchor Roles

```text
AnchorA.PNG = layout grammar anchor
AnchorB.PNG = style DNA anchor
AnchorC.PNG = asset / object vocabulary anchor
```

These anchors are the current TP01 canonical baseline unless a later committed version replaces them. They define role grammar only and must not be treated as current-run extracted content.
